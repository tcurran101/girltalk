          <%= simple_form_for @photo, url: '#' do |f| %>
            <%= f.input :image %>
            <%= f.input :caption %>
            <%= f.submit "Share Photo", class: 'btn btn-primary' %>
         <% end %>
